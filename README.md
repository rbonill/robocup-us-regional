RoboCup US @2026

This is a working copy of `robocup-org/robocup-us-regional`, which publishes the official page at https://us.robocup.org.
**GitHub Pages is turned off here** (2026-09-15): the only public site is the official one. `CNAME` matches the official
repo on purpose so syncing from it stays conflict-free; `index.html` keeps a `rel="canonical"` link to https://us.robocup.org/
in case this copy is ever published again.

**us.robocupamericas.org** is not served from here: FastComet answers it with a 301 redirect to https://us.robocup.org/
(folder `~/us.robocupamericas.org`, 2026-09-15).
