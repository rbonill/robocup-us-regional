RoboCup US @2026

This is a copy of `robocup-org/robocup-us-regional`, which publishes the official page at https://us.robocup.org.
`CNAME` matches the official repo on purpose: GitHub gives a domain to only one repository, so this copy has no custom
domain and is only visible at https://rbonill.github.io/robocup-us-regional/. `index.html` keeps a `rel="canonical"` link to
https://us.robocup.org/ so search engines treat the official page as the original.

**us.robocupamericas.org** is not served from here: FastComet answers it with a 301 redirect to https://us.robocup.org/
(folder `~/us.robocupamericas.org`, 2026-09-15).
